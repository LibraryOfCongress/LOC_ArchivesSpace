# LOC PUI Branding

Various PUI templates, JS, and CSS to customize the PUI for LOC.

Note that this plugin is somewhat intertwined with LOC data model 
extensions, so it is probably not usable as-is for other installs.
