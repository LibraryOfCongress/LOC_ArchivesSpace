<% Array(note['content']).each do |content| %>
  <%= process_mixed_content(content) %>
  <br/>
<% end %>

<ul>
  <% Array(note['items']).each do |item| %>
    <li><%= process_mixed_content( item ) %></li>
  <% end %>
</ul>
